#!/bin/bash

VAULT=${VAULT:-$HOME/.local/bin/vault}

if [ -e "$VAULT" ] && [ -n "$ZSH_NAME" ]; then
  # setup vault autocomplete
  export VAULT
  autoload -U +X bashcompinit && bashcompinit
  complete -o nospace -C $VAULT vault
fi
